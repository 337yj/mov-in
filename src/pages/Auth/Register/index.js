import React from "react";
import styles from "./register.module.scss";

const Register = () => {
    return(
        <main>
            <section className={styles.wrapper}>
                this is signup page(temporary)
            </section>
        </main>
    );
};

export default Register;