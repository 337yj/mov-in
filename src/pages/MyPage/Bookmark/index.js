import React from "react";
import styles from "./bookmark.module.scss";

const Bookmark = () => {
  return (
    <section className={styles.wrapper}>
      <h1 className={styles.title}>북마크 페이지</h1>
    </section>
  );
};

export default Bookmark;
