import React from "react";
import styles from "./userInfo.module.scss";

const UserInfo = () => {
  return (
    <section className={styles.wrapper}>
      <h1 className={styles.title}>회원정보 페이지</h1>
    </section>
  );
};

export default UserInfo;
